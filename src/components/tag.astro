---
type Props = {
  tag: string;
};

const { tag } = Astro.props;
---

<a
  href={`/tags/${encodeURI(tag)}`}
  target="_self"
  class="px-2.5 py-1 rounded-lg text-xs text-neutral-200 font-medium border capitalize"
>
  {tag}
</a>
