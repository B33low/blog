diff --git a/MEDIA_GUIDE.md b/MEDIA_GUIDE.md
index eb1933e..25b8b93 100644
--- a/MEDIA_GUIDE.md
+++ b/MEDIA_GUIDE.md
@@ -98,3 +98,22 @@ Local images can still be placed under `src/content/images/` and handled by `ast
 6. Upload the public derivatives to `hexadecilab-media` and reference them in the film frontmatter.
 
 The Astro site never transcodes video. It only selects which R2 object the browser should play.
+
+## Large photos: lightweight gallery previews
+
+Remote photos are served by R2 as-is, so a 25 MB PNG used directly in a gallery card would cost 25 MB before the visitor even opens it. Photo entries now support an optional `thumbnail` field:
+
+```yaml
+image: "https://media.b33low.com/photos/Remontee.png"
+thumbnail: "https://media.b33low.com/photos/previews/Remontee.webp"
+```
+
+`thumbnail` is used by the Photography grids/homepage. `image` remains the full-resolution source used when the visitor opens the lightbox or photo page. Existing entries without `thumbnail` keep working and fall back to `image`.
+
+A small helper script publishes both versions through the existing Ant Media utility container:
+
+```bash
+./scripts/publish-photo.sh ~/Pictures/Remontee.png
+```
+
+It keeps the original unchanged and generates a max-1600px WebP preview at quality 82 before uploading both files to R2.
diff --git a/scripts/publish-photo.sh b/scripts/publish-photo.sh
new file mode 100755
index 0000000..a1ab780
--- /dev/null
+++ b/scripts/publish-photo.sh
@@ -0,0 +1,58 @@
+#!/usr/bin/env bash
+set -Eeuo pipefail
+
+# Publish a large photo to R2 and create a lightweight gallery preview using
+# the existing Ant Media utility container (ffmpeg + rclone).
+#
+# Usage:
+#   ./scripts/publish-photo.sh /path/to/Remontee.png
+#   ./scripts/publish-photo.sh /path/to/Remontee.png Remontee
+
+INPUT="${1:?Usage: $0 /path/to/photo [name]}"
+NAME="${2:-$(basename "${INPUT%.*}")}"
+EXT="${INPUT##*.}"
+CONTAINER="${ANTMEDIA_CONTAINER:-antmedia}"
+BUCKET="${R2_MEDIA_BUCKET:-hexadecilab-media}"
+PUBLIC_BASE="${MEDIA_PUBLIC_BASE:-https://media.b33low.com}"
+RCLONE_CONFIG="/opt/hexadecilab/rclone.conf"
+TMP="/tmp/hexadecilab-photo-${NAME}"
+
+if [[ ! -f "$INPUT" ]]; then
+  echo "File not found: $INPUT" >&2
+  exit 1
+fi
+
+cleanup() {
+  podman exec "$CONTAINER" rm -rf "$TMP" >/dev/null 2>&1 || true
+}
+trap cleanup EXIT
+
+podman exec "$CONTAINER" mkdir -p "$TMP"
+podman cp "$INPUT" "$CONTAINER:$TMP/original.$EXT"
+
+echo "Creating WebP preview (max 1600 px)..."
+podman exec "$CONTAINER" ffmpeg \
+  -hide_banner -loglevel warning -y \
+  -i "$TMP/original.$EXT" \
+  -vf "scale='min(1600,iw)':'min(1600,ih)':force_original_aspect_ratio=decrease" \
+  -frames:v 1 \
+  -c:v libwebp \
+  -quality 82 \
+  -compression_level 6 \
+  "$TMP/preview.webp"
+
+echo "Uploading original..."
+podman exec "$CONTAINER" rclone \
+  --config "$RCLONE_CONFIG" \
+  copyto "$TMP/original.$EXT" \
+  "r2:${BUCKET}/photos/${NAME}.${EXT}"
+
+echo "Uploading preview..."
+podman exec "$CONTAINER" rclone \
+  --config "$RCLONE_CONFIG" \
+  copyto "$TMP/preview.webp" \
+  "r2:${BUCKET}/photos/previews/${NAME}.webp"
+
+printf '\nPublished. Use this in Astro:\n\n'
+printf 'image: "%s/photos/%s.%s"\n' "$PUBLIC_BASE" "$NAME" "$EXT"
+printf 'thumbnail: "%s/photos/previews/%s.webp"\n' "$PUBLIC_BASE" "$NAME"
diff --git a/src/components/media/photo-card.astro b/src/components/media/photo-card.astro
index 73e145f..17e796b 100644
--- a/src/components/media/photo-card.astro
+++ b/src/components/media/photo-card.astro
@@ -11,7 +11,9 @@ type Props = {
 
 const { photo, tall = false } = Astro.props;
 const image = photo.data.image;
+const thumbnail = photo.data.thumbnail ?? image;
 const isImageObject = typeof image === "object" && (image as any)?.src;
+const isThumbnailObject = typeof thumbnail === "object" && (thumbnail as any)?.src;
 const fullSrc = isImageObject ? (image as any).src : (image as string);
 const tagList = photo.data.tags ?? [];
 const meta = [photo.data.location, photo.data.date].filter(Boolean).join(" · ");
@@ -27,16 +29,19 @@ const meta = [photo.data.location, photo.data.date].filter(Boolean).join(" · ")
   class:list={["group relative block overflow-hidden rounded-lg bg-zinc-800", tall ? "row-span-2 aspect-[3/4]" : "aspect-square"]}
 >
   {
-    isImageObject ? (
+    isThumbnailObject ? (
       <Image
-        src={image as any}
+        src={thumbnail as any}
         alt={photo.data.title}
+        loading="lazy"
         class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"
       />
     ) : (
       <img
-        src={image as string}
+        src={thumbnail as string}
         alt={photo.data.title}
+        loading="lazy"
+        decoding="async"
         class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"
       />
     )
diff --git a/src/components/media/photo-teaser-grid.astro b/src/components/media/photo-teaser-grid.astro
index 2639ef4..bf04a64 100644
--- a/src/components/media/photo-teaser-grid.astro
+++ b/src/components/media/photo-teaser-grid.astro
@@ -14,9 +14,12 @@ const [a, b, c, d] = photos;
 
 const cardProps = (photo: CollectionEntry<CollectionType.Photos>) => {
   const image = photo.data.image;
+  const thumbnail = photo.data.thumbnail ?? image;
   const isImageObject = typeof image === "object" && (image as any)?.src;
+  const isThumbnailObject = typeof thumbnail === "object" && (thumbnail as any)?.src;
   return {
-    isImageObject,
+    thumbnail,
+    isThumbnailObject,
     fullSrc: isImageObject ? (image as any).src : (image as string),
     meta: [photo.data.location, photo.data.date].filter(Boolean).join(" · "),
   };
@@ -34,10 +37,10 @@ const cardProps = (photo: CollectionEntry<CollectionType.Photos>) => {
         data-meta={cardProps(a).meta}
         class="group relative md:col-span-2 md:row-span-2 overflow-hidden rounded-lg bg-zinc-800 aspect-[4/5] md:aspect-auto"
       >
-        {cardProps(a).isImageObject ? (
-          <Image src={a.data.image as any} alt={a.data.title} class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105" />
+        {cardProps(a).isThumbnailObject ? (
+          <Image src={cardProps(a).thumbnail as any} alt={a.data.title} loading="lazy" class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105" />
         ) : (
-          <img src={a.data.image as string} alt={a.data.title} class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105" />
+          <img src={cardProps(a).thumbnail as string} alt={a.data.title} loading="lazy" decoding="async" class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105" />
         )}
         <div class="absolute bottom-0 left-0 right-0 p-4 bg-gradient-to-t from-black/70 to-transparent">
           <p class="text-white font-semibold">{a.data.title}</p>
@@ -55,10 +58,10 @@ const cardProps = (photo: CollectionEntry<CollectionType.Photos>) => {
             data-meta={cardProps(photo).meta}
             class="group relative overflow-hidden rounded-lg bg-zinc-800 aspect-square md:aspect-auto"
           >
-            {cardProps(photo).isImageObject ? (
-              <Image src={photo.data.image} alt={photo.data.title} class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105" />
+            {cardProps(photo).isThumbnailObject ? (
+              <Image src={cardProps(photo).thumbnail as any} alt={photo.data.title} loading="lazy" class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105" />
             ) : (
-              <img src={photo.data.image} alt={photo.data.title} class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105" />
+              <img src={cardProps(photo).thumbnail as string} alt={photo.data.title} loading="lazy" decoding="async" class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105" />
             )}
             <div class="absolute bottom-0 left-0 right-0 p-3 bg-gradient-to-t from-black/70 to-transparent">
               <p class="text-white text-sm font-semibold">{photo.data.title}</p>
@@ -76,10 +79,10 @@ const cardProps = (photo: CollectionEntry<CollectionType.Photos>) => {
           data-meta={cardProps(d).meta}
           class="group relative md:col-span-1 md:row-span-2 overflow-hidden rounded-lg bg-zinc-800 aspect-[4/5] md:aspect-auto"
         >
-          {cardProps(d).isImageObject ? (
-            <Image src={d.data.image as any} alt={d.data.title} class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105" />
+          {cardProps(d).isThumbnailObject ? (
+            <Image src={cardProps(d).thumbnail as any} alt={d.data.title} loading="lazy" class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105" />
           ) : (
-            <img src={d.data.image as string} alt={d.data.title} class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105" />
+            <img src={cardProps(d).thumbnail as string} alt={d.data.title} loading="lazy" decoding="async" class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105" />
           )}
           <div class="absolute bottom-0 left-0 right-0 p-4 bg-gradient-to-t from-black/70 to-transparent">
             <p class="text-white font-semibold">{d.data.title}</p>
diff --git a/src/content/config.ts b/src/content/config.ts
index 60b58ea..d8318ee 100644
--- a/src/content/config.ts
+++ b/src/content/config.ts
@@ -97,8 +97,14 @@ const photos = defineCollection({
           }),
         ),
       tags: z.array(z.string()).optional(),
-      // display image; can point straight at an R2 url to skip Astro's re-encoding
+      // Full-resolution image. Remote R2 URLs are served as-is and are only loaded
+      // when the visitor explicitly opens the photo/detail page.
       image: image().or(z.string()),
+
+      // Optional lightweight preview used by gallery cards/homepage. For large R2
+      // originals, point this at a pre-generated WebP/AVIF thumbnail.
+      thumbnail: image().or(z.string()).optional(),
+
       location: z.string().optional(),
     }),
 });
